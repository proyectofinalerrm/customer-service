spring:
  application:
    name: customer-service
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8095
  
management:
  endpoints:
    web:
      exposure:
        include: "*"

